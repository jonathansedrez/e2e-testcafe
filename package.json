{
  "name": "basic-testcafe-example",
  "version": "0.1.0",
  "description": "Basic TestCafe API example",
  "main": "tests/test.js",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/DevExpress/testcafe/issues"
  },
  "scripts": {
    "docker:build": "docker build --platform linux/amd64 -t my-testcafe .",
    "docker:run": "docker run --rm --platform linux/amd64 -v $PWD:/tests -v $PWD/reports:/tests/reports my-testcafe \"chromium:headless\" /tests/tests --screenshots /tests/reports/screenshots --video /tests/reports/videos"
  },
  "devDependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "testcafe": "*"
  }
}